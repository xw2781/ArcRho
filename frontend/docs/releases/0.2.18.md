# Release 0.2.18

Released on 2026-06-02.

## User-Facing Changes

### Improvements
- **dataset**: Let the Dataset Details formula box expand vertically for longer formulas.
- **dataset**: Moved the Dataset project inputs into a separate Details tab frame.
- **project instance**: Project Instance now avoids duplicate cached dataset folder scans when loading a selected reserving class.

## Fragment Sources

- `changes/unreleased/dataset_details_formula_autosize.json`
- `changes/unreleased/dataset_details_input_frame.json`
- `changes/unreleased/project_instance_cached_dataset_request_sharing.json`
