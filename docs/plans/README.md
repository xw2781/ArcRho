# Plans

Cross-component plans live here. A plan whose work has fully landed moves to
[completed/](completed/) and is kept as the record of the decisions behind the
shipped behaviour rather than deleted. Per-page and per-method plans live in
[frontend/docs/plans/](../../frontend/docs/plans/) instead.

Give every plan a `Status:` line under its title and keep it current; this
index is a summary of those lines, not a second source of truth.

## Open

| Plan | Status |
| :--- | :--- |
| [build_new_methods.md](build_new_methods.md) | Four of five ResQ methods shipped; Bootstrap Consolidation is still open. Also holds the ResQ API and sample-project reference notes agents rely on. |
| [excel_addin_gateway_transport.md](excel_addin_gateway_transport.md) | Investigated and decided 2026-09-12 after proving VBA can sign a Gateway request byte-exactly; broken into 10 session-sized steps covering a per-recalculation cache, the add-in's signing module, a values-returning hosted operation, the switch off the share, a credential the add-in installs for itself on first run, and the release; no decision is open — the speed question was answered on 2026-09-12, the plan goes on because the values match and the reasons for the move do not depend on the timing, and the credential question was answered the same day with automatic first-run installation, which became step 10 and runs before steps 8 and 9; 9 of 10 done, the per-recalculation cache, the add-in's signed Gateway client, the hosted operation that answers with a dataset's figures, the same answer for a project's period headings and settings, worksheet formulas reading through the Gateway, the deletion of the share path itself, the removal of the one producer that wrote a coarser view to disk, the credential a PC now installs for itself when Excel opens, and the one-page user note at [excel-addin/README.md](../../excel-addin/README.md) all landed 2026-09-12, with every figure identical on both paths and the server about twice as fast on a dataset the Engine has to calculate; only the release remains. |
| [generated_formula_dependency_refresh.md](generated_formula_dependency_refresh.md) | Investigated 2026-09-05; broken into 7 session-sized steps on 2026-09-11 covering the shared formula graph, sidecar links, the scoped Engine refresh, the Project Instance Dependency Graph window, and a one-off script that fixes the existing projects; 6 of 7 done, the one shared reader of a dataset-type formula, the sidecar links that use it, the ResQ import writing the same links, the scoped import rebuilding the formula datasets built on the types it was narrowed to, the Dependency Graph drawing those links and marking a generated formula, and the one-off script that repairs the projects that already exist all landed 2026-09-12; the Bridge, the Engine and the Gateway were deployed 2026-09-13, leaving the repair runs and the app checks on the Server PC. |
| [hosted_save_http_transport.md](hosted_save_http_transport.md) | Implemented for every hosted-save kind; TLS, traffic limits, and retiring the SMB path remain. |
| [hosted_workspace_http_transport.md](hosted_workspace_http_transport.md) | Phase 1 reads and Phase 2 engine calculations implemented; the bounded-server foundation, SSE, and small writes remain. |
| [local_runtime_log_retention_plan.md](local_runtime_log_retention_plan.md) | Audit complete (2026-08-09); remediation not started. |
| [multiple_app_instances.md](multiple_app_instances.md) | Diagnosed 2026-09-13 after a ResQ export failed with two ArcRho apps on one PC, one of which had killed the other's app server and taken its port; widened the same day from the macro fix alone to full support for two windows side by side, and broken into 7 session-sized steps covering coexisting app servers, the owner that travels with a macro command, the window that hands back work it cannot own, a server whose life follows its windows, an address book that lists every running app, the measurement of what a second window can remember, and the two-app check with the deploy; estimated at 325 minutes of agent time; no decisions open, none started. |

## Completed

| Plan | Landed |
| :--- | :--- |
| [completed/custom_data_processing.md](completed/custom_data_processing.md) | 2026-07-16 — custom data processing rules and their Project Settings editor. |
| [completed/engine_dependent_propagation_plan.md](completed/engine_dependent_propagation_plan.md) | 2026-08-06 — both phases of the Engine-hosted dependent propagation job. |
| [completed/hosted_rpc_bridge_transport.md](completed/hosted_rpc_bridge_transport.md) | 2026-08-19 — DFM and Result Selection sync moved off SMB, except the deliberately deferred `apply`. |
| [completed/persisted_json_contract_v4.md](completed/persisted_json_contract_v4.md) | 2026-08-23 — every stored JSON file moved to one `snake_case` convention with fewer fields and one audit-log policy, converted in place for `NJ_Annual_Prod_202605_Fake` (the other projects are re-imported from ResQ instead) and released as ArcRho 1.3.3; closed 2026-09-06 with the ultra review dropped and the release left unforced, since every user is assumed to run the latest version. |
| [completed/method_calculation_precision_parity.md](completed/method_calculation_precision_parity.md) | 2026-09-08 — every method now stores and calculates with a number at the precision it was observed with, so a DFM chain, a Bornhuetter-Ferguson, a Cape Cod and a Result Selection reproduce ResQ instead of a six-decimal projection of it. Supersedes the DFM average-row import diagnosis. |
| [completed/manual_input_period_rollup.md](completed/manual_input_period_rollup.md) | 2026-09-05 — a hand-entered triangle now records the shape it is really stored at, a coarser view of it is added up along the calendar on every open instead of being cached beside it, and a yearly method can take a monthly or quarterly one as its input. Includes the [backfill run report](completed/manual_input_period_rollup_backfill_report.md) for the 10,997 records already on the server. |
| [completed/manual_input_stored_length_resq_alignment.md](completed/manual_input_stored_length_resq_alignment.md) | 2026-09-07 — a hand-entered triangle can now be stored finer than it is shown, a coarser development view can be typed, pasted and linked into with each figure written at its column's age and the periods between cleared, and the export writes the triangle to ResQ at ArcRho's stored shape. The ResQ stored-length rules it recorded have since been re-probed and widened into [reference/resq_stored_and_display_lengths.md](../reference/resq_stored_and_display_lengths.md), which is the reference to read. |
